# 07 SFT 监督微调

## 1. 这一节要解决什么问题

这一节让你理解 SFT 如何用“输入和标准答案”训练模型，并能准备适合 SFT 的指令数据。

## 2. 基础概念解释

### SFT

通俗解释：SFT 像老师拿标准答案批改作业，学生反复模仿正确解法。

技术解释：SFT 是 Supervised Fine-tuning，用带有目标输出的数据训练模型，使模型在给定输入下生成期望回答。

例子：输入“解释什么是 Tokenizer”，目标输出是面向初学者的解释。

### Instruction tuning 指令微调

通俗解释：指令微调像教模型“听懂任务说明并照做”。

技术解释：Instruction tuning 是用大量指令、输入、回答样例训练模型，提高模型遵循用户指令的能力。

例子：指令可以是“总结下面文本”“翻译成英文”“抽取公司名和金额”。

### Alpaca 格式

通俗解释：Alpaca 格式像三栏作业卡：任务要求、补充材料、参考答案。

技术解释：Alpaca 常用字段是 `instruction`、`input`、`output`。

例子：

```json
{"instruction":"解释 LoRA","input":"","output":"LoRA 是一种参数高效微调方法。"}
```

### ShareGPT 格式

通俗解释：ShareGPT 格式像聊天记录，一轮一轮记录用户和助手说了什么。

技术解释：ShareGPT 常用 `conversations` 数组保存多轮对话，每条包含角色和内容。

例子：

```json
{
  "conversations": [
    {"from": "human", "value": "什么是 SFT？"},
    {"from": "gpt", "value": "SFT 是监督微调，用标准答案训练模型。"}
  ]
}
```

## 3. 为什么要学这个

SFT 是大多数微调项目的起点。LoRA 和 QLoRA 只是训练方式，真正决定模型学什么的是 SFT 数据。如果你不会构造 SFT 数据，就算训练命令跑通，模型也可能学不到你想要的能力。

## 4. 关键知识点

### 4.1 SFT 学的是什么

解释：SFT 学的是“看到某种输入时，应该输出什么样的答案”。

例子：如果训练数据总是用“通俗解释、技术解释、例子”三段回答，模型就更可能学会这种结构。

注意事项：SFT 不适合无脑堆资料。资料要变成任务样例，例如问答、摘要、抽取、分类。

### 4.2 Alpaca 格式怎么用

解释：Alpaca 适合单轮指令任务。

例子：

```jsonl
{"instruction":"请解释 Fine-tuning","input":"","output":"Fine-tuning 是在已有模型基础上继续训练，让模型适配特定任务。"}
{"instruction":"从文本中抽取产品名","input":"用户购买了 iPhone 15 Pro。","output":"iPhone 15 Pro"}
```

注意事项：`instruction` 写任务，`input` 写材料，`output` 写答案。不要把所有内容都塞进一个字段。

### 4.3 ShareGPT 格式怎么用

解释：ShareGPT 适合多轮对话，尤其是聊天助手。

例子：

```jsonl
{"conversations":[{"from":"human","value":"什么是 LoRA？"},{"from":"gpt","value":"LoRA 是一种参数高效微调方法。"},{"from":"human","value":"它为什么省显存？"},{"from":"gpt","value":"因为它冻结基础模型，只训练少量新增参数。"}]}
```

注意事项：多轮数据要保证上下文连贯。不要让第二轮回答依赖不存在的第一轮信息。

### 4.4 数据质量标准

解释：SFT 数据要正确、稳定、统一、覆盖真实场景。

例子：

| 标准 | 好样例 | 差样例 |
| --- | --- | --- |
| 正确 | 概念准确 | 把 LoRA 说成 RAG |
| 稳定 | 格式一致 | 一会儿列表一会儿散文 |
| 清晰 | 问题明确 | “讲讲这个” |
| 覆盖 | 包含常见和边界问题 | 全是同一种问法 |

注意事项：宁可 300 条高质量数据，也不要 3000 条混乱数据。

### 4.5 SFT 和 LoRA 的组合

解释：实际项目常说“LoRA 微调”，很多时候指“用 LoRA 的方式做 SFT”。

例子：

```text
训练目标：SFT，让模型模仿标准答案。
训练方式：LoRA，只训练少量参数。
数据格式：Alpaca 或 ShareGPT。
```

注意事项：面试时要说清楚这三层，否则容易被追问。

## 5. 和前后知识的关系

前面学习了 LoRA 和 QLoRA 的训练方式，本节讲“训练内容”。后面的数据清洗会告诉你如何把原始资料变成 SFT 数据，训练参数和日志会告诉你训练时如何观察效果。

## 6. 实战任务

围绕“Fine-tuning 学习助手”写 10 条 Alpaca 格式数据，要求：

1. 至少 5 条是概念解释。
2. 至少 3 条是场景判断。
3. 至少 2 条是报错排查。
4. 每条 output 都包含通俗解释或例子。

## 7. 检查自己是否学会

1. SFT 和 Instruction tuning 有什么关系？
2. Alpaca 格式三个字段分别放什么？
3. ShareGPT 格式适合什么任务？
4. SFT 数据为什么不能只放原始文档？
5. 什么样的答案会被模型学坏？
6. 为什么格式统一很重要？
7. 如何解释“用 LoRA 做 SFT”？
8. 数据量和数据质量哪个更重要？

## 8. 常见误区

1. 误区：SFT 数据就是把文档切成段落。  
   解释：SFT 要有输入和目标输出，原始段落更像继续预训练或 RAG 知识库材料。

2. 误区：答案越长越好。  
   解释：答案长度要服务任务。过长会让模型学会啰嗦。

3. 误区：多轮数据越多越好。  
   解释：多轮数据质量差会让模型上下文混乱。

4. 误区：只要格式是 JSONL 就能训练好。  
   解释：JSONL 只是文件格式，真正关键是内容质量。

## 9. 本节总结

SFT 是让模型模仿标准答案的核心训练方式。Alpaca 适合单轮指令，ShareGPT 适合多轮对话。初学者做项目时，最常见组合是“Alpaca/ShareGPT 数据 + LoRA/QLoRA 训练 + SFT 目标”。

